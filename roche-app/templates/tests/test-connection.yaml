apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "roche-app.fullname" . }}-test-connection"
  labels:
    {{- include "roche-app.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "roche-app.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
